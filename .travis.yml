language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
os:
  - linux
  - osx
compiler:
  - gcc
  
before_install:
  - sudo apt-get install libgl1-mesa-dev

install:
  - pip install -r requirements.txt
  - pip install -e .
  
jobs:
  include:
    - stage: test
      script: make test
    - stage: coverage
      os: linux
      script: make coverage
      #TODO: connect to codecov.io
    - stage: doc
      os: linux
      script: make docs
      #TODO: publish to github pages and readthedocs.io
    - stage: deploy
      os: linux
      script: make deploy
      #TODO: Upload to Github Releases and maybe even Play Store
      
